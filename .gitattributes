# Auto detect text files and perform LF normalization
* text=auto

# Bash по SSH ожидает LF; иначе на Windows в репо попадают CRLF и удалённый bash падает.
backend/app/worker/scripts/*.sh text eol=lf
