worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /tmp/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    sendfile on;
    tcp_nopush on;
    keepalive_timeout 65;
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    map $http_x_forwarded_proto $forwarded_proto {
        default $http_x_forwarded_proto;
        ''      $scheme;
    }

    server {
        listen 80;
        server_name _;
        root /usr/share/nginx/html;
        index index.html;

        add_header X-Content-Type-Options nosniff always;
        add_header Referrer-Policy strict-origin-when-cross-origin always;

        location /api {
            proxy_pass http://api:8000;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $forwarded_proto;
            proxy_set_header X-Forwarded-Host $host;
            proxy_read_timeout 120s;
            proxy_connect_timeout 30s;
        }

        # Статика Vue (страница /sub/:token/open/:client).
        location ^~ /assets/ {
            root /usr/share/nginx/sub-app;
            try_files $uri =404;
            expires 7d;
            add_header Cache-Control "public, immutable";
        }

        # Страница открытия VPN-клиента — Vue SPA, не проксировать на API (иначе 302 на тот же URL).
        location ~ ^/sub/[^/]+/open/[^/]+$ {
            root /usr/share/nginx/sub-app;
            try_files $uri /index.html;
        }

        # Остальное под /sub — публичное API (Base64, JSON, …/open/…/data, geoip).
        location /sub {
            proxy_pass http://api:8000;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $forwarded_proto;
            proxy_set_header X-Forwarded-Host $host;
        }

        # Заглушка «сайт в разработке».
        location / {
            try_files $uri /index.html;
        }
    }
}
